# HaloWebUI Android

Native Android WebView wrapper for HaloWebUI.

Build from this directory or the repository root:

```bash
ANDROID_HOME=/opt/android-sdk gradle -p android assembleDebug
```

The debug APK is generated at:

```text
android/app/build/outputs/apk/debug/app-debug.apk
```
